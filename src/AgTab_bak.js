
				/*		.filter(row => {
									if (!props.filtVal) return true
									if (row.ProjectCode.includes(props.filtVal) || row.ProjectName.includes(props.filtVal)) 
									{
									return true
									}
							})		*/
					/*	.map((row,index) => 
							return (
									<tr key={index}>
										<td>{row.id}</td>
										<td>{row.ProjectCode}</td>	  
										<td>{row.ProjectName}</td>				  
										<td>
											<button onClick={() => props.removeAgProject(index)}>x</button>
										</td>
										<td>
											<input type="checkbox" checked={row.Enabled} onChange={() => props.handleEnabled(index)}/>
										</td>	
										<td>
											<button onClick={() => props.projectDetails(row.ProjectCode)}>View Details</button>
										</td>						
									</tr>
									);
							)*/
				//	});